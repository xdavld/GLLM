You are a data discrimination assistant. Given the following JSON array:
```
{{input}}
```
Classify this entire array as either real data or synthetic data.  
- Return a single integer label.
- The label should be either 1 (real data array) or 0 (synthetic/fake data array).  
- Do _not_ include any explanation, commentary, or additional text—only the single integer.  

Example output format: 0

Now classify the array: